import type { ComparisonResult } from "../lib/gemini";
export function mountCompareView(host: HTMLElement): void { host.innerHTML='<section class="mt-8 rounded-xl border border-slate-700 p-6"><h2 class="text-2xl font-bold">Compare two documents</h2><div class="mt-4 grid gap-4 md:grid-cols-2"><textarea id="compare-a" class="min-h-40 rounded border p-3" aria-label="First document text" placeholder="First document text"></textarea><textarea id="compare-b" class="min-h-40 rounded border p-3" aria-label="Second document text" placeholder="Second document text"></textarea></div><button id="compare-button" class="mt-4 rounded bg-teal-300 px-4 py-2 font-bold text-slate-950">Compare</button><div id="compare-output" class="mt-4" role="status"></div></section>'; const output=host.querySelector<HTMLElement>("#compare-output")!; host.querySelector<HTMLButtonElement>("#compare-button")!.onclick=async()=>{const first=host.querySelector<HTMLTextAreaElement>("#compare-a")!.value;const second=host.querySelector<HTMLTextAreaElement>("#compare-b")!.value;if(first.length<20||second.length<20){output.textContent="Add both documents before comparing.";return;}output.textContent="Comparing…";const response=await fetch("/api/compare",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({first,second})});const payload=await response.json() as {data?:ComparisonResult};if(!payload.data){output.textContent="Comparison unavailable.";return;}output.innerHTML='<p class="font-bold">'+payload.data.summary+'</p>'+payload.data.differences.map(item=>'<article class="mt-3 rounded border p-3 '+item.significance+'"><b>'+item.significance+': '+item.clause+'</b><p>A: '+item.docA+'</p><p>B: '+item.docB+'</p></article>').join('');}; }