import type { VercelRequest,VercelResponse } from "@vercel/node";import { z } from "zod";import { buildChatPrompt, callGeminiJson } from "../src/lib/gemini";import { allowRequest,parseBody,postOnly } from "./_shared";
const schema=z.object({documentText:z.string().min(20).max(60000),question:z.string().min(3).max(500),history:z.array(z.object({role:z.enum(["user","assistant"]),content:z.string().max(1000)})).max(8).default([])});
export default async function handler(req:VercelRequest,res:VercelResponse):Promise<void>{if(!postOnly(req,res)||!allowRequest(req,res))return;const body=parseBody(schema,req,res);if(!body)return;try{const key=process.env.GEMINI_API_KEY;if(!key)throw new Error();const raw=await callGeminiJson<{answer:string}>(buildChatPrompt(body.documentText,body.question,(body.history ?? [])),key);res.status(200).json({data:raw.answer});}catch{res.status(200).json({data:"Assistant unavailable. Please review the document directly; this is general information, not legal advice.",fallback:true});}}